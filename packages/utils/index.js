import {
  formatOptions
} from './funcs.js'

import {
  baseForm,
  baseTable,
  baseDialog,
  baseModel
} from './model.js'

import {
  initModel,
  initTable,
  initDialog,
  initForm,
  initFormRules,
  initDefaultForm,
  triggers
} from './modelUtils.js'

export {
  formatOptions,
  baseForm,
  baseTable,
  baseDialog,
  baseModel,
  initModel,
  initTable,
  initDialog,
  initForm,
  initFormRules,
  initDefaultForm,
  triggers
}

export default {
  formatOptions,
  baseForm,
  baseTable,
  baseDialog,
  baseModel,
  initModel,
  initTable,
  initDialog,
  initForm,
  initFormRules,
  initDefaultForm,
  triggers
}
